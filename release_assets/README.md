# Release Assets

This directory documents large files intended for GitHub Release upload.

Do not commit large assets directly to the GitHub repository.

Prepared asset:

```text
tinyjudou_1200k_longmix_v1.json.tar.gz
```

Upload it through GitHub Releases, then keep the repository itself small.
